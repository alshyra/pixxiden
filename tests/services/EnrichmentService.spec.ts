import { describe, it } from "vitest";

describe("EnrichmentService", () => {
  it.todo("should enrich a game with IGDB + ProtonDB + SteamGridDB data");
  it.todo("should skip locked overrides during enrichment");
});
