{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for PixiDen",
  "windows": [
    "main",
    "splashscreen",
    "game-overlay"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-create",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:event:default",
    "shell:allow-execute",
    "shell:allow-open"
  ],
  "scope": {
    "allow": [
      "$HOME/.config/pixiden/**",
      "$HOME/.config/legendary/**",
      "$HOME/.config/gogdl/**",
      "$HOME/.config/nile/**",
      "$HOME/.config/heroic/**",
      "$HOME/.local/share/pixiden/**",
      "$HOME/.local/share/wine-ge/**",
      "$HOME/.local/share/proton-ge/**",
      "$HOME/GOG Games/**",
      "$HOME/Games/**",
      "$RESOURCE/binaries/*"
    ],
    "sidecar": [
      "binaries/legendary",
      "binaries/nile",
      "binaries/gogdl"
    ]
  }
}